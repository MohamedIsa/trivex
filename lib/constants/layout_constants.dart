// Named layout constants — replaces all magic numbers in widget code.

/// Horizontal page padding (symmetric).
const double kScreenPaddingH = 24.0;

/// Standard card / section inner padding.
const double kCardPadding = 24.0;

/// Card corner radius (rounded-2xl).
const double kCardRadius = 16.0;

/// Button corner radius (rounded-xl).
const double kButtonRadius = 12.0;

/// Chip / pill corner radius — fully rounded.
const double kChipRadius = 99.0;

/// A / B / C / D answer badge width & height.
const double kBadgeSize = 32.0;

/// Accessibility minimum tap-target dimension.
const double kMinTapTarget = 48.0;

/// Timer progress bar height.
const double kTimerBarHeight = 4.0;

/// Default icon size inside badges and result circles.
const double kIconSize = 32.0;

/// Large icon size on the reveal sheet result circle.
const double kResultIconSize = 64.0;
